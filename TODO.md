todo:

- finance logging
- media scraping: auto-fetch cover art / basic info for entries and backlog
  items (e.g. games via RAWG, movies/shows via TMDB, books via Open
  Library/Google Books) — needs a per-category API key in Settings since
  LifeLog has no backend
- activity heatmap: GitHub-style calendar grid showing entry density by
  month/year
- year in review: shareable summary card for a chosen year (top categories,
  totals, achievements)
- settings fixes:
  - the tab bar inside settings shows a vertical scrollbar — remove it
  - rewrite the description text in each settings section to be clearer and
    use bullet points where appropriate
- stats: "unique entries" count should show a "Unique Entries" label on hover
  just like the other stat numbers do
- bottom bar / active-tab label:
  - remove the arrow icon from the active tab label
  - when tapping the active tab label to open the tabs menu, size and anchor
    the menu to the label button (not full-width)
- filter panel: align the padding between the category filter label and the
  year filter label so they start at the same indent level
