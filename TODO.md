todo:

- make sure when loading and there is a version diff between local/drive/git show a selection and time of each of them for better control

- mobile: the bottom nav bar feels a bit short/cramped - make it a bit taller

- the + (add) button and the settings (gear) button use different colors - make them match (same color)
