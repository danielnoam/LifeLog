todo:

- finance logging
- privacy / app lock: optional PIN, passkey, or biometric (fingerprint/Face ID via WebAuthn) to unlock the app on open
- filter bar: clicking the "Years" or "Categories" label should select all chips; if everything is already selected, clicking again should deselect all
