todo:

- finance logging
- sync with Steam wishlist: pull in wishlist items to add to the Media/Games backlog — likely blocked by the same CORS issue that killed Steam's title search (confirmed in 0.19.0), so this would need a manual import (e.g. paste a wishlist export) or a backend proxy, not a direct browser fetch
