todo:

- mobile view: move the top bar to the bottom and keep it always visible - left side has Timeline / Categories / Stats tabs, right side has a + (add) button and a settings (gear) button

- make sure when loading and there is a version diff between local/drive/git show a selection and time of each of them for better control
