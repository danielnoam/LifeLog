# Your actual log data — never publish these. Data lives in the private
# lifelog-data repo (cloud sync) and/or your local file.
lifelog.json
data/

# The native build. www/ is copied from the app by tools/build-www.js, and
# android/ is generated by Capacitor in CI — both are outputs, not sources.
node_modules/
/www/
/android/
